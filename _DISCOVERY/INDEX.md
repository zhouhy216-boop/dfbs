# _DISCOVERY / INDEX
> 目的：集中存放“讨论到一半/暂停中的需求（THREAD）”，避免遗忘与重复沟通。  
> 使用方式：每个需求一份 THREAD 文件；以后继续讨论时，直接把对应 THREAD 文件上传/更新即可。

---

## 1) THREAD 索引（进行中 / 暂停）
| THREAD 文件 | 标题 | 状态 | 一句话说明 | 下次继续需要的实物 |
|---|---|---|---|---|
| `THREADS/THREAD_全局测试数据清理器.md` | 全局测试数据清理器 | 已冻结 v1 | 用户名旁边一键/多选清理测试数据；依赖自动补全；全清含组织架构；附件默认不清 | 模块清单与命名；“当前模块”判定方式；清理后最小可测状态口径 |
| `THREADS/THREAD_表单自定义轮子.md` | 表单自定义轮子（表单+节点字段+按钮可配置+按钮动作引擎） | 进行中 | 管理员可配置字段/顺序/校验；不同节点不同字段与按钮；按钮条件可配；按钮动作统一口子 | 各流程节点列表（谁在哪个节点填什么）；发货/委托表单雏形或字段清单 |
| `THREADS/THREAD_角色与权限轮子.md` | 角色与权限轮子（Role/User Permission） | 进行中 | 角色模板(推荐)+账号加减(最终生效)；全量权限树；保存生效；开发验证两套都要 | 权限点初版清单（可先从平台申请开始）；超管/管理员规则；测试账号套件规划 |
| `THREADS/THREAD_确认提交与规则提醒轮子.md` | 确认提交与规则提醒轮子（Confirm/Warning Gate） | 进行中 | 最终提交/通过前统一弹确认/规则提醒；摘要字段由超管配置；必须弹（不提供“以后不再提示”） | 1-2 个“提交确认”示例（哪些按钮弹+展示字段）；Confirm 弹框展示偏好 |
| `THREADS/THREAD_智能粘贴解析.md` | 智能粘贴解析 | 暂停 | 先做发货/委托表单，再做字段映射与解析扩展 | 发货/委托/工单字段清单 + 粘贴样本 + 委托类型清单 |

---

## 2) 轮子路线图（待讨论→再开干，不先建 THREAD）
> 说明：这里是“按我理解的推荐开发顺序”排的队。  
> 我们接下来按顺序一个个讨论清楚；讨论清楚并冻结口径后，再决定：要不要建 THREAD / 或直接发给 DFBS-Delivery 开干。  
> （如果某项暂时只有想法、没有具体需求，就先放这里，不建 THREAD。）

### 2026-02-11 推荐顺序（从“越早越省返工”到“越后越增值”）
1. **DICT-01 数据字典管理轮子**（下拉选项/类型/状态统一维护）
2. **PERM-01 动态角色权限轮子**（按钮/字段级；角色模板+账号加减；保存生效）
3. **AUDIT-01 审计日志轮子**（谁在什么时候改了什么）
4. **HIST-01 操作历史回溯/版本与回滚**（可选：是否并入 AUDIT 或独立）
5. **FLOW-01 流程配置轮子**（流转步骤/谁处理/条件跳转——依赖权限更清晰）
6. **COMP-01 通用表格与导出轮子**（分页/排序/过滤/批量操作/导出）
7. **SRCH-01 搜索过滤面板轮子**（高级搜索面板/快捷条件）
8. **BATCH-01 批量导入导出模板管理轮子**（模板/校验/错误报告）
9. **ATTACH-CORE 附件基础层轮子**（统一上传/下载/预览/安全/生命周期；module/type/owner/tenant 等）
10. **ATTACH-01 附件预览与水印轮子**
11. **NOTIFY-01 短信/邮件通知模板轮子**
12. **NOTI-01 通知消息中心轮子**（铃铛+未读+消息列表）
13. **PRINT-01 打印模板轮子**（报价单/运单/工单/PDF 导出）
14. **RPT-01 报表与看板中心**
15. **PM-01 预防性维护/续保提醒**
16. **PORT-01 客户自助门户/小程序**

---

## 3) 规则文档导航（权威输入）
| 文档路径 | 用途 |
|---|---|
| `_DISCOVERY/rules/platform_application_rules.md` | 平台申请流程规则报告 v1（节点按钮矩阵/字段权限矩阵/类型差异矩阵，含 AND/OR 条件表达式） |
| `_DISCOVERY/rules/platform_application_role_permission_matrix_v1.md` | 平台申请“角色/权限矩阵 v1”（代码事实：当前未接入 RBAC，差异由 stage/sourceType + status + 弹窗状态驱动） |

---

## 4) 新增 THREAD 的规范（统一、好找、不会乱）
- 存放路径：`_DISCOVERY/THREADS/`
- 文件命名：`THREAD_<需求名>.md`（例：`THREAD_发货表单.md`）
- 每个 THREAD 至少包含 4 段（建议照此写，方便续聊）：
  1) **已冻结**（不再反复讨论）
  2) **未确认（Unknown）**（下次继续的重点）
  3) **已给出的样本/例子**（原话或原文粘贴）
  4) **下次需要的实物**（字段清单/样本/截图/现有流程等）

---

## 5) 续聊规则（避免重复问）
- 继续讨论某个需求时：以对应 THREAD 文件为准，只推进“未确认/待实物”的部分。
- THREAD 里标为“已冻结”的内容：默认不再重复确认，除非你明确说要改。

---
