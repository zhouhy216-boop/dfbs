# FORMWHEEL-260312-002-S01.b2 交付说明

## Completed?
Yes

## Build/test status
- 未改动「新建机构」或表单轮子逻辑；仅补充注释并明确边界。ReadLints：PASS。

## Is 「新建机构」 part of the same Step-01 source family in local repo reality?
**No**

- **依据**：Step-01 抽取与接线的来源为 **Platform Application** 的「创建申请」表单（`src/pages/Platform/Application/index.tsx` 内 ModalForm），对应入口为：销售申请、营企申请、服务申请（及申请平台）。「新建机构」位于 **Platform Org** 页面（`src/pages/Platform/Org/index.tsx`），由 `openCreateModal` 打开的是**创建机构**弹窗（POST `/v1/platform-orgs`），与「创建申请」是不同页面、不同表单、不同接口，故不属于同一 Step-01 来源家族。

## What was done (boundary clarification, no fake consistency)
- **未**在「新建机构」上接入表单轮子或可见验收钩子，避免将 Step-01 范围扩大到机构创建表单。
- 在 **Platform Application** 两处 Create ModalForm（createModalOnly 与 Tabs 内）的 FormContainer 上方增加**注释**，明确 Step-01 表单轮子验收范围仅为「销售申请/营企申请/服务申请」创建入口，并注明平台管理页「新建机构」为机构创建表单、不在本步验收范围。
- 通过注释与本文 receipt 将「实际 Step-01 验收边界」写清，避免用户误以为「新建机构」也应在 Step-01 内有一致钩子。

## Current Step-01 user-visible scope after fix (short list)
- **在 Step-01 验收范围内的入口**（同一表单家族，已具备可见验收钩子）：
  - 平台申请相关：**销售申请**、**营企申请**、**服务申请**（及 **申请平台**，enterprise direct 时）
  - 入口位置：平台申请页「新建申请」弹窗，或 createModalOnly 下的同一弹窗（标题随 sourceType 变化）
- **不在 Step-01 验收范围内的入口**（不同表单家族，本步不做一致性要求）：
  - **新建机构**：平台管理（Platform Org）页的机构创建弹窗，与上述「申请创建」表单非同一来源家族。

## What to verify (1–3 bullets; UI-oriented)
1. 在**平台申请**入口打开「新建申请」：仍可见「基本信息」分组、说明文案、草稿提示与共用字段校验（销售/营企/服务申请标题下行为一致）。
2. 在**平台管理**页点击「新建机构」：保持原有机构创建流程与界面不变；不要求与申请创建表单呈现相同 Step-01 钩子。
3. 验收 Step-01 时，仅以「销售申请 / 营企申请 / 服务申请」创建入口为范围，不以「新建机构」作为 Step-01 一致性检查对象。

## Blocker question
None.
