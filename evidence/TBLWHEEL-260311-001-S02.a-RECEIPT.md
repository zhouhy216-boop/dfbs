# TBLWHEEL-260311-001-S02.a 交付说明

## Completed?
Yes

## Build/test status (only what is relevant to this ticket)
- `npx tsc --noEmit`（前端）：全项目存在既有 TS 报错（BasicLayout、OrgTree、ImportCenter、Quote 等未改动的文件）；本次 Step-02 改动的表格相关页面与 `UnifiedProTable` 未新增类型错误。
- 未执行 `npm run build` 全量构建（因既有错误会先失败）；仅针对本次改动的导入与组件替换做了本地校验。

## Changed areas (very short)
- **共享轮子**：`src/shared/table/UnifiedProTable.tsx` 增加全部 Step-02 用到的 `UNIFIED_TABLE_KEYS`。
- **已迁移页面**：Quote、Shipment、AfterSales、Finance、Warehouse/Inventory、Warehouse/Replenish（两个 Tab）、Platform/Org、Platform/Application、MasterData（SimCard、SparePart、Machine、MachineModel、ModelPartList）、Admin/DictionaryTypes、Admin/DictionaryItems、System/PlatformConfig、WorkOrder/Internal（四个 Tab）。
- **文案**：Quote 新建按钮改为「新建报价单」；ModelPartList 表头改为「型号零件清单」；AfterSales 移除未使用导入。

## Actual rollout scope derived from local reality (very short)
- **已纳入**：根据当前路由与页面，所有以 ProTable 为主列表的用户可见列表页均已迁移到 `UnifiedProTable` + 独立 `tableKey`，包括：客户、报价单、发运、售后、回款、库存、补货（我的申请/待我审批）、平台机构、平台申请、SIM 卡、配件、设备、机型、型号零件清单、数据字典类型、数据字典项、平台配置、工单（待受理/待派单/我的工单/全部工单）；账户权限页已在 Step-01 使用 UnifiedProTable，未再改动。
- **未纳入**：Detail 页内嵌表格（如 Machine/Detail、SimCard/Detail、MachineModel/Detail 中的 ProTable）仍为原生 ProTable，未替换；ConfirmationCenter、OrgLevelConfig、OrgTree、OrgChangeLog、DictionaryTransitions、DictionarySnapshotDemo、ImportCenter、ApplicationsHistory、PlatformApply 等非主列表或非 ProTable 页面未动。

## Step-02 rollout result (very short)
- **一致 phase-1 控制语言**：是；上述列表页均具备刷新、密度、列设置、全屏、恢复默认、列宽拖拽、结果数/刷新时间、统一空值/加载/空结果等 phase-1 能力。
- **非 ProTable 离群页**：账户权限已在 Step-01 统一；本次未再发现其他主列表仍用裸 Table 的离群页。
- **结构上不适用的例外**：无；分页/页容量在具备分页的接口上均可用；工单四个 Tab 各自独立 tableKey，列状态与列宽按 Tab 分别持久化。

## What to verify (1–3 bullets; UI-oriented)
1. 在各模块打开上述列表页（客户、报价、发运、售后、回款、库存、补货、平台机构/申请、主数据各列表、数据字典类型/项、平台配置、工单四个 Tab），确认工具栏一致：刷新、密度、列设置、全屏、恢复默认，以及结果数与刷新时间、列宽拖拽、单元格复制（在已用 CopyableCell 的列上）。
2. 切换密度、调整列宽、恢复默认视图后，刷新或切路由再回来，确认列状态与列宽按 tableKey 持久化。
3. 确认未改动的页面可见性与权限行为不变；仅表格交互统一，无路由/权限语义变更。

## 1 blocker question max
无。
