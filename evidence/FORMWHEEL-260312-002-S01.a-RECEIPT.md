# FORMWHEEL-260312-002-S01.a 交付说明

## Completed?
Yes

## Build/test status
- `npx tsc --noEmit`: 全项目仍有既有 TS 报错（非本次改动文件）；`src/shared/form/*` 与 Platform Application 本次改动处无新增错误。已修复 FormFields 未使用 `Input` 的 import。

## Changed areas
- **共享轮子**：新增 `src/shared/form/`：FormSection、FormContainer、DraftAlert、formWheelStyles.css、useFormReadonly、ReadonlyFormView、FormFields（FormFieldPhone/FormFieldEmail/FormFieldText）、formValidators（复用小写 validators）、useFormTemplate、index.ts。
- **来源表单**：`src/pages/Platform/Application/index.tsx` 中两处 Create ModalForm（createModalOnly 与 Tabs 内）接入 FormContainer、DraftAlert、FormSection「基本信息」、FormFieldPhone、FormFieldEmail；草稿条与电话/邮箱改为走表单轮子。

## Extraction result
- **Form container extracted?** Yes. FormContainer + DEFAULT_FORM_LAYOUT。
- **Grouped/sectioned carrying extracted?** Yes. FormSection（title / help / body）。
- **Common field rendering extracted?** Yes. FormFieldPhone、FormFieldEmail、FormFieldText（基于 SmartInput + 统一校验）。
- **Basic validation extracted?** Yes. formValidators 复用小写 PhoneRule/EmailRule；表单项内嵌 required + 规则。
- **Draft capability extracted?** Yes. DraftAlert 组件 + 继续使用既有 useDraftForm；Create 弹窗已用 DraftAlert。
- **Whole-form readonly/editable switching extracted?** Yes. useFormReadonly + ReadonlyFormView 已入轮子；本步未在 Platform Application 上挂切换控件。
- **Default template carrying extracted?** Yes. useFormTemplate(formRef, template) 已入轮子；业务可用来填默认/草稿值。
- **Help text / annotation carrying extracted?** Yes. FormSection 的 help、FormFieldText 的 extra。

## Current mismatch / unresolved unknowns
- None. 未引入工作流、审计、权限矩阵、设计器语义。

## What to verify later in UI (future-facing)
1. 进入平台申请入口，打开「新建申请」：可见「基本信息」分组、草稿提示（恢复/清除）、电话/邮箱为统一字段样式与校验。
2. 后续 Step 02 可在同一入口验证：只读/可编辑切换（若挂上 useFormReadonly + ReadonlyFormView）、模板/说明文案展示。
3. Contract Review V1 后续可直接复用 shared/form 的容器、分组、通用字段、草稿条、只读视图与模板 hook。

## Blocker question
None.
