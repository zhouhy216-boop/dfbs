# TBLWHEEL-260311-001-S01.a 交付说明

**Ticket ID:** TBLWHEEL-260311-001-S01.a  
**Title:** Build phase-1 unified table foundation across confirmed ProTable-shaped pages and account-permissions outlier

---

## Completed? Yes

## Build/test status

- `npx tsc --noEmit`: **PASS**（本票涉及文件无 TS 报错）
- 全量 `npm run build`：仍存在**与本次改动无关**的既有 TS 错误（OrgTreeSelect、BasicLayout、BizPermCatalogMaintenance、OrgTree、RolesPermissions、AfterSales、ImportCenter、Machine/Detail、ConflictDrawer、Platform/Application、Quote、PlatformConfig 等），未修复，不影响本步交付范围。

## Changed areas

- **共享轮子**
  - `frontend/dfbs-ui/src/shared/table/` 新增：
    - `unifiedTableLocale.ts`：中文空状态/加载/空值占位
    - `unifiedTableConstants.ts`：分页、滚动默认
    - `useTableColumnsState.ts`：列状态持久化（localStorage，按 tableKey）、恢复默认
    - `UnifiedProTable.tsx`：ProTable 包装，注入 options/columnsState/locale/scroll/pagination/rowClassName/toolBarRender（结果数、刷新时间、恢复默认）、斑马纹
    - `unifiedTableStyles.css`：斑马纹、行悬停
    - `CopyableCell.tsx`：可复制单元格组件
    - `index.ts`：导出
- **已接入的页面**
  - `Customer/index.tsx`：改为使用 `UnifiedProTable`，tableKey=`UNIFIED_TABLE_KEYS.CUSTOMER`
  - `MasterData/Contract/index.tsx`：改为使用 `UnifiedProTable`，tableKey=`UNIFIED_TABLE_KEYS.CONTRACT`
  - `Admin/AccountPermissions/AccountsTab.tsx`：账号列表由 antd `Table` 改为 `UnifiedProTable`，数据流改为 `request` + `params.keyword`，刷新统一用 `accountTableRef.current?.reload()`，tableKey=`UNIFIED_TABLE_KEYS.ACCOUNT_LIST`

## Confirmed touched Step 01 families

- **ProTable 族（已接入）：** 客户列表（`/customers`）、合同列表（`/master-data/contracts`）
- **account-permissions 族（已接入）：** `/admin/account-permissions` 账号列表表格（由原 antd Table 迁移为 UnifiedProTable，同一套 phase-1 交互）

## Phase-1 contract result

- **通过共享轮子已实现：**
  - 列设置（ProTable setting）、密度、刷新、全屏
  - 列状态持久化（localStorage，按 tableKey）、恢复默认视图（工具栏按钮）
  - 固定表头（scroll.y）、横向滚动（scroll.x: max-content）
  - 斑马纹、行悬停（CSS）
  - 统一空值占位（columnEmptyText）、统一 locale（空/加载等中文）
  - 分页与每页条数切换（pageSizeOptions 10/20/50）、showTotal「共 N 条」
  - 结果数 + 刷新时间提示（工具栏）、加载/空结果由 ProTable 统一展示
  - 单元格复制：共享 `CopyableCell` 组件；ProTable 列可配置 `copyable`（列 copyable 由各页按需使用）
- **持久化范围：** 仅列显示/顺序等 columnsState，按页面 tableKey 存 localStorage；无后端持久化。
- **不适用/例外：**
  - **列宽拖拽调整（direct UI column-width adjustment）：** 未实现；当前 ProTable/antd Table 无现成列拖拽调整，本步未引入第三方 resizable，在 receipt 中记为例外，可后续迭代。
  - **固定首列/操作列：** 未在共享层强制；各页可通过列 `fixed` 按需配置；本步未对固定列做统一默认。
  - **account-permissions 表格：** 无服务端分页，`pagination={false}`，故无每页条数切换；其余 phase-1 能力（列设置、密度、刷新、恢复默认、斑马纹、空状态、结果数/刷新时间）与 ProTable 族一致。

## What to verify（UI 向）

1. 打开**客户**（如 `/customers`）和**合同**（如 `/master-data/contracts`）：工具栏可见刷新、密度、列设置、全屏；可切换密度、打开列设置、刷新；表格有斑马纹与行悬停；分页可切换每页条数，底部有「共 N 条」；点击「恢复默认视图」后列状态恢复并刷新；再次进入页面列状态保持。
2. 打开**账号与权限**（`/admin/account-permissions`）账号列表：与上述同一套工具栏与表格样式（列设置、密度、刷新、恢复默认、斑马纹、空状态、结果数/刷新时间）；搜索后刷新列表行为正常；创建/启用停用/用户名更正/绑定人员更正后列表通过刷新更新。

## Blocker question

无。

---

*列宽拖拽调整已记录为 phase-1 例外，后续可单独迭代。*
