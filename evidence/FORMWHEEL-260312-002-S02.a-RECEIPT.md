# FORMWHEEL-260312-002-S02.a 执行收据

## Completed? Yes

## Build/test status
- `npx tsc --noEmit`: 本票修改已通过；项目中仍有其他文件既有 TS 报错（非本票引入）。
- `ReadLints` on `Application/index.tsx`: PASS.

## 选定的代表入口及原因
- **入口**：平台申请创建弹窗（申请管理 → 待处理 → 新建申请，或 销售申请/营企申请/服务申请 任一入口打开的同一创建弹窗）。
- **原因**：Step-01 已确认的“平台申请创建”来源家族即为此弹窗（销售/营企/服务共用同一 Create ModalForm）；业务表面清晰、入口单一，适合作为 Step-02 唯一验收面。

## 在该入口上的验收结果（简要）
| 能力 | 是否可见可验 |
|------|----------------|
| form container 承载 | Yes |
| 分组/区块展示 | Yes（基本信息 + 只读时「基本信息（只读）」） |
| 通用字段渲染 | Yes（电话/邮箱等共享字段 + 合同号/单价台数原因等） |
| 基础校验 | Yes（必填、合同号等） |
| 草稿能力 | Yes（保存草稿 / 恢复草稿 / 清除草稿） |
| 整表只读/可编辑切换 | Yes（「只读预览」⇄「返回编辑」） |
| 默认模板承载 | Yes（「恢复默认」按钮，加载默认值并 toast） |
| 说明/帮助文案 | Yes（分组 help、DraftAlert 文案） |

## 本入口新增或调整的可见验收点
- 底部增加「恢复默认」：将表单部分字段恢复为默认值并提示「已恢复默认」。
- 底部增加「只读预览」：当前填写内容以只读形式展示；底部变为「返回编辑」可切回编辑。
- 只读态下：分组标题为「基本信息（只读）」、说明为「当前为只读预览，点击「返回编辑」继续填写。」，表体为 `ReadonlyFormView` 展示当前快照。

## 未验收 / 阻塞项
- None. 只读切换与默认模板均在当前入口通过可见 UI 验收，未引入新产品语义。

## 建议验收步骤（UI 向）
1. 进入 申请管理 → 待处理 → 新建申请（或从 销售/营企/服务 入口打开同一创建弹窗）。
2. 查看：分组「基本信息」、说明文案、草稿提示（若有草稿）；填写必填项并触发校验；点击「保存草稿」后关闭再打开，恢复/清除草稿。
3. 点击「恢复默认」：观察部分字段被重置并出现「已恢复默认」提示。
4. 点击「只读预览」：观察整表变为只读展示；点击「返回编辑」恢复编辑。

## Blocker 问题
- 无。
