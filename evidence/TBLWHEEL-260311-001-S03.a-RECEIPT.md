# TBLWHEEL-260311-001-S03.a 交付说明

## Completed?
Yes

## Build/test status (only what is relevant to this ticket)
- `npx tsc --noEmit`（前端）：全项目仍存在既有 TS 报错（OrgTreeSelect、BasicLayout、OrgTree、Quote、Platform 等未在本次清理中改动的文件）；本次 Step-03 改动的文件（ConfirmationCenter、Machine/Detail、SimCard/Detail、MachineModel/Detail、ImportCenter、UnifiedProTable tableKeys）未新增类型错误。

## Changed areas (very short)
- **共享轮子**：`src/shared/table/UnifiedProTable.tsx` 新增 4 个 tableKey：CONFIRMATION_CENTER、MACHINE_DETAIL_HISTORY、SIMCARD_DETAIL_HISTORY、MACHINEMODEL_DETAIL_BOM。
- **ConfirmationCenter**：6 个 Tab 内原 antd `Table` 改为 `UnifiedProTable`，使用统一 tableKey、request 从 pool 取数，pool 更新时通过 ref 触发 reload；列类型改为 ProColumns。
- **MasterData/Machine/Detail**：归属历史 Tab 内原 `ProTable` 改为 `UnifiedProTable`，options=false、toolBarRender=false，request 返回 historyData，historyData 变化时 reload。
- **MasterData/SimCard/Detail**：绑定历史 Tab 内原 `ProTable` 改为 `UnifiedProTable`，同上。
- **MasterData/MachineModel/Detail**：零部件清单 Tab 内原 `ProTable` 改为 `UnifiedProTable`，保留 request 与 toolBarRender（新建版本）；标签改为「零部件清单」，弹窗标题改为「新建版本」。
- **ImportCenter**：失败明细、冲突处理两处 antd `Table` 增加 `className="unified-table"`，并引入 `unifiedTableStyles.css`，使展示与统一表格样式一致。

## Retirement result (very short)
- **冲突的旧表格交互是否已移除/中和**：是。数据确认中心由裸 Table 改为 UnifiedProTable；设备/SIM 卡/机型详情内嵌表格由裸 ProTable 改为 UnifiedProTable；导入中心结果区 Table 仅保留展示用途并套用统一样式类，无第二套工具栏。
- **交付后是否仅剩一条主表格路径**：是。上述相关列表/表格页均走统一 phase-1 路径或统一样式，用户不再看到并行的旧表格系统。
- **是否仍有用户可见例外**：无。OrgPeople 未在现行路由中暴露，未纳入本次清理；ApplicationsHistory 当前为占位 Tabs 无表格，未动。

## Actual touched cleanup scope (very short)
- 页面/模块：Admin/ConfirmationCenter，MasterData/Machine/Detail、SimCard/Detail、MachineModel/Detail，ImportCenter；共享表 UNIFIED_TABLE_KEYS。

## What to verify (1–3 bullets; UI-oriented)
1. 打开数据确认中心，切换各实体 Tab，确认表格具备统一工具栏（刷新、密度、列设置、全屏、恢复默认等），与其它列表页一致；确认/修正后数据刷新正常。
2. 打开设备详情、SIM 卡详情、机型详情，查看「归属历史」「绑定历史」「零部件清单」子表：前两者无工具栏、样式与统一表一致；零部件清单具备完整工具栏与「新建版本」按钮，交互与主列表一致。
3. 导入中心执行导入后，失败明细与冲突处理两表展示风格与统一表格一致（斑马纹等），且无多余工具栏。

## 1 blocker question max
无。
