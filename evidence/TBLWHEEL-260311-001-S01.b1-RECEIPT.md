# TBLWHEEL-260311-001-S01.b1 交付说明（Bugfix）

**Ticket ID:** TBLWHEEL-260311-001-S01.b1  
**Title:** Fix phase-1 unified table gaps found in UI acceptance for touched Step-01 pages only

---

## Completed? Yes

## Build/test status

- `npx tsc --noEmit`：本票涉及文件（shared/table、Customer、Contract、AccountsTab）**PASS**
- 全量 `npm run build`：仍存在与本次改动无关的既有 TS 错误（见基线），**未修复**；不影响本 bugfix 范围。

## Changed areas

- **共享轮子**
  - `shared/table/UnifiedProTable.tsx`：恢复默认立即生效（restoreKey  remount + 清列宽持久化）、恢复默认改为图标+Tooltip、列宽拖拽（mergedColumns + ResizableTitle + components.header.cell）、列宽持久化与恢复时清除
  - `shared/table/useTableColumnsState.ts`：列宽读写与清除（getColumnWidthsStorageKey、getStoredColumnWidths、setStoredColumnWidths、clearColumnWidthsStorage）
  - `shared/table/ResizableTitle.tsx`：新增，表头列宽拖拽（react-resizable）
  - `shared/table/CopyableCell.tsx`：复制成功时 `message.success('已复制')`
- **依赖**
  - `package.json`：新增 `react-resizable`、`@types/react-resizable`
- **已触及页面**
  - `Customer/index.tsx`：客户编码、名称列使用 CopyableCell
  - `MasterData/Contract/index.tsx`：合同号列使用 CopyableCell
  - `Admin/AccountPermissions/AccountsTab.tsx`：移除 `size="small"` 以让密度生效；用户名、昵称列使用 CopyableCell

## Bugfix result

- **account-permissions 密度是否生效？** Yes（已移除固定 `size="small"`，由 ProTable 密度开关控制）
- **恢复默认是否立即生效？** Yes（清持久化 + 清列宽存储 + setRestoreKey 触发 ProTable remount，无需手动刷新）
- **恢复默认是否为图标+Tooltip？** Yes（UndoOutlined + Tooltip「恢复默认视图」，与刷新/密度/列设置/全屏同一工具栏风格）
- **是否存在直接列宽拖拽？** Yes（表头拖拽调整列宽，列宽按 tableKey 持久化，恢复默认时清除）
- **是否存在专用复制交互与反馈？** Yes（CopyableCell 复制后 `message.success('已复制')`；客户/合同/账号列表关键列已接 CopyableCell）

## Touched-page consistency note

- **Customer、Contract、account-permissions** 均通过同一 `UnifiedProTable` 获得：刷新/密度/列设置/全屏/恢复默认（图标+Tooltip）、列宽拖拽、斑马纹、空状态、结果数/刷新时间、可复制列（CopyableCell + 已复制 toast）。三页工具栏与表格交互一致，均为 phase-1 统一契约。

## What to verify（UI 向）

1. **账号与权限**：切换密度（紧凑/默认/宽松）后表格行高立即变化；点击恢复默认（撤销图标）后列状态与列宽立即恢复，无需刷新页面。
2. **客户 / 合同 / 账号与权限**：表头有宽度的列可拖拽边缘调整列宽，刷新或再进入页面列宽保持；点击单元格复制图标后出现「已复制」提示。
3. **三页**：恢复默认为工具栏图标+悬停「恢复默认视图」，与刷新/密度/列设置/全屏同一风格。

## Blocker question

无。

---

*列宽拖拽依赖 react-resizable；仅对带 width 的列启用 onHeaderCell+ResizableTitle。*
