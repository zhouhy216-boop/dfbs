# PROJECT_PARKING_LOT.md（需求停车场｜已确认但暂不做）

> 用途：记录“讨论很清楚，但为了整体推进先不做”的需求，避免以后重新聊。
> 规则：只追加；每条给一个 ID；写清“暂不做原因”和“未来验收口径”。
## 待补录清点区（来自旧讨论，暂不进入开发）
规则：先记录，不争论；等成为“唯一推进点”再做 Interview → Logic Freeze → Dev&Test。

- YYYY-MM-DD：一句话（你想到什么就写什么）
  - 你担心的风险/原因：
  - 你希望的结果：
  - 当前状态：待补录

---

## 记录格式（固定）

### [ID] 标题
- 状态：PARKED（暂不做）/ READY（随时可做）/ DONE（已实现）
- 暂不做原因：一句话说明为什么这轮不做
- 业务口径（冻结）：把规则写清楚（不写代码）
- 未来验收口径：User 怎么判断“做对了”

---

## 已停车条目

### [QI-CFG-001] QuoteItem：费用类型/费用描述/单位 的“管理员可配置”
- 状态：PARKED
- 暂不做原因：本轮先跑通 QuoteItem 最小闭环，避免卡在配置后台与权限上
- 业务口径（冻结）：
  - 费用类型：目前有固定类型（如维修费/上门费/配件费/平台费/流量费/仓储费/运输费/包装费/施工费等）
  - 未来需要：管理员可新增类型、可改名、可停用
  - 费用描述：不是自由文本；是下拉；且“不同费用类型的下拉选项不同”；管理员可配置/修改
  - 单位：不同费用类型单位不同（配件=个；平台费=点；流量=月/年；其他=次等），未来由管理员维护
- 未来验收口径：
  - 能维护“费用类型字典”
  - 能维护“费用描述字典”，并支持按费用类型分组
  - QuoteItem 选择费用类型后，费用描述下拉随之变化
  - 单位能随费用类型自动推荐/限制

### [QI-AUTO-001] QuoteItem：配件类自动带出 BOM 图号（规格型号）
- 状态：PARKED
- 暂不做原因：依赖 BOM 与“机型+配件名称”的联动查询，本轮先不引入复杂依赖
- 业务口径（冻结）：
  - 当费用类型=配件费：
    - 费用描述=配件名称（可选）
    - 规格型号=配件图号（来自 BOM）
    - 规则：按“机型 + 配件名称”自动带出
- 未来验收口径：
  - 选机型+配件名称后，规格型号自动出现且正确

### [QI-AUTO-002] QuoteItem：平台费自动带出机构代码（规格型号）
- 状态：PARKED
- 暂不做原因：机构代码来源与关联规则需单独确认，先不阻塞 QuoteItem 闭环
- 业务口径（冻结）：
  - 当费用类型=平台费：
    - 规格型号字段填写“机构代码”
    - 未来希望能自动带出
- 未来验收口径：
  - 平台费行能自动带出机构代码，且可追溯来源

### [QI-CUSTOM-001] QuoteItem：预留至少 10 个自定义字段（后续启用）
- 状态：PARKED
- 暂不做原因：先预留结构/不启用 UI，避免本轮 UI 与导出复杂化
- 业务口径（冻结）：
  - 至少 10 个自定义字段（后续由管理员启用/命名/决定类型）
- 未来验收口径：
  - 管理员可启用字段并配置名称/类型
  - QuoteItem 表单能显示这些字段并保存导出

- 2026-01-26 报价单字段“确认前可临时输入，CONFIRMED 必须归一到字典项”扩展
  - 背景/原因：希望维护更友好、统计口径更稳定；确认后数据必须规范化。
  - 期望结果：除“银行信息、办事处”外，报价单其它字段尽量支持：确认前可临时手填；CONFIRMED 时必须替换为系统字典/主数据选项；缺失时由有权限的人新增字典项后再确认。
  - 备注：本轮先落地到 费用类型/单位/统计分类/规格型号（含配件 BOM 归一）。
  - 当前结论：暂存（未进入本轮推进点扩展范围）

- 2026-01-26 完整 BOM（机型→配件清单）关系
  - 背景/原因：让报价单配件规格型号能按机型自动推荐，减少手工选错。
  - 期望结果：每个机型可维护配件清单；工单发起报价单时可按机型/工单配件自动带出候选；CONFIRMED 时配件必须归一到配件库/机型BOM。
  - 当前结论：暂存（后续单独作为推进点）

- 2026-01-26 报价单回款确认流程（执行人填回款信息 → 财务确认已回款）
  - 背景/原因：财务最终确认后进入收款执行；回款完成后不允许再更改收款执行人；回款信息用于统计/开票/入账。
  - 期望结果：
    1) 执行人提交回款信息：选择汇款方式、填写回款金额、选择回款时间、上传回款截图（可选）
    2) 流程流转到财务：财务核对到账后点击“已回款”
    3) 财务点“已回款”后：禁止再更改“收款执行人”
    4) 支持多次回款（分次回款记录）
  - 权限与流转：
    - 执行人：只能提交/更新“回款信息”（在财务确认已回款前）
    - 财务：确认到账并点击“已回款”
  - 当前结论：暂存（下一轮推进点候选）

- 2026-01-26 报价单抄送领导（可见全流程进度）
  - 背景/原因：财务确认后需要领导知情，领导能随时查看报价单后续进度（回款/作废/后续补单等）。
  - 期望结果：报价单进入关键节点（提交财务/财务确认/回款确认/作废等）自动通知领导；领导可在系统内查看该报价单全进程。
  - 备注：领导抄送对象可能按部门/项目配置；需要权限控制。

- 2026-01-26 发件报价单抄送仓库
  - 背景/原因：涉及发货/出库的报价单，需要仓库提前同步信息。
  - 期望结果：当报价单满足“需要发件/发货”条件时，自动抄送仓库；仓库侧可看到待处理清单与关键信息。
  - 备注：需要定义“需要发件”的触发条件（字段/勾选/费用类型等）。

- 2026-01-26 报价单发票申请分支流程
  - 背景/原因：回款后可能需要走开票申请，且与报价/回款存在分支联动。
  - 期望结果：支持从报价单发起开票申请；与回款状态关联（例如未回款不允许开票/或仅允许预申请）；形成审批/流转。
  - 备注：需明确开票信息字段、审批人、开票完成回写等。

