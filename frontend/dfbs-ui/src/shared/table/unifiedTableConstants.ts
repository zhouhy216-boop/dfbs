/**
 * Phase-1 统一表格：分页、滚动等默认配置
 */

/** 默认每页条数选项 */
export const UNIFIED_PAGE_SIZE_OPTIONS = [10, 20, 50];

/** 默认每页条数 */
export const UNIFIED_DEFAULT_PAGE_SIZE = 10;

/** 固定表头默认高度（视口内滚动） */
export const UNIFIED_SCROLL_Y = 400;
