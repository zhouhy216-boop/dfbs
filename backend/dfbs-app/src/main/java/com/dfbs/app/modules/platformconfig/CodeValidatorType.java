package com.dfbs.app.modules.platformconfig;

/**
 * Org code validation rule: NONE, UPPERCASE (letters only), CHINESE (汉字), MIXED (uppercase + Chinese).
 */
public enum CodeValidatorType {
    NONE,
    UPPERCASE,
    CHINESE,
    MIXED
}
