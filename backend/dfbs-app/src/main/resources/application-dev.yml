# Loaded when spring.profiles.active includes "dev". Local/dev only; not used in prod.
dfbs:
  dev:
    seedNonAllowlistUser: true
  perm:
    # Enable test utilities (Role-Vision) in dev for verification. Prod never loads this file.
    testUtilitiesEnabled: true
