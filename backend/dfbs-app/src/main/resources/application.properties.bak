server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dfbs
    username: dfbs
    password: dfbs
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info